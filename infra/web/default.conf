server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    server_tokens off;
    charset utf-8;
    # HTML and route fallbacks must revalidate after each release.
    add_header Cache-Control 'no-cache';

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 'ok';
    }

    location /images/ {
        try_files $uri =404;
        add_header Cache-Control 'public, max-age=86400';
    }

    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control 'public, max-age=31536000, immutable';
    }


    location / {
        try_files $uri $uri/index.html /index.html;
    }
}
