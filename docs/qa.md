# Проверки «Сладкого Дара»

Даты проверок: 10–11 сентября 2026. Node 22.20, pnpm 10.15.1; React 18.3.1, TypeScript strict. Проверки выполнены локально, результаты не являются обещанием полевых Core Web Vitals.

## Правки оптового сайта и переход на VK — 11 сентября 2026

- Обновлены формулировки hero, преимуществ, поводов, происхождения продукции, уровней опта и сроков сборки. Удалены три неактуальных повода из карточек главной, продуктовых данных и фильтров каталога. Пять карточек поводов расположены в сетке 3 + 2 на desktop.
- В витрине состав следует сразу за прокручиваемым списком; примечание о фото перенесено вниз. На 1440 px список и описание занимают около 538 px, зазор между ними — около 7 px. На маленьком экране подробности остаются читаемыми в одной колонке.
- Перетаскивание мышью начинается после горизонтального движения на 8 px, не вызывает случайный выбор набора, обычный следующий клик работает. Для мыши отключён snap, мешавший короткому перетаскиванию; touch сохраняет нативный scroll и snap. Клавиатурная навигация и источник заявки сохранены.
- Проверены реальные touch-события Chrome через CDP на 375 × 900: горизонтальный свайп изменил scrollLeft 6 → 310 без вертикальной прокрутки; вертикальный жест изменил pageY 5212 → 5395 без изменения горизонтального положения. Самопроизвольного выбора или открытия формы нет.
- Полный Playwright: 18/18 passed на семи ширинах. После последней правки продуктовых поводов повторены 4 затронутых сценария (4/4 passed): фильтры/пустая выдача/сброс, новые тексты, drag и keyboard/источник заявки.
- Frontend Vitest: 18/18 passed; после последних продуктовых данных повторены 9 тестов каталога и витрины, 9/9 passed. Имя и телефон остаются обязательными, комментарий необязателен.
- Визуальная проверка витрины и пяти поводов: 1440, 1024, 768, 375 px; нет переполнения страницы. axe-core: 0 нарушений WCAG 2 A/AA и 2.1 AA на каждой из четырёх ширин. Артефакты — ignored .cache/v3-*.jpg и .cache/qa-v3.json.
- API: 62/62 теста прошли на реальной PostgreSQL (30 проверок integration, 30 транспорта VK и 2 миграций). Проверены стабильный random_id при повторе, ошибки VK в HTTP 200, таймауты, отсутствие токена в URL/логах и сохранение истории при миграции.
- Общие ESLint, TypeScript и format:check прошли; web и API собираются. Реальная отправка в VK до настройки адресата и токена не выполнялась.

Описания прежних прогонов ниже относятся к состоянию проекта на момент тех проверок. Упоминания прежнего транспорта в них являются историей QA; текущая интеграция и параметры настройки описаны в [инструкции VK](vk-setup.md).

## Упрощение формы — 11 сентября 2026

- Имя и телефон остаются обязательными; комментарий допускает отсутствие, пустую строку или пробелы и сохраняется как `NULL`.
- Shared contract: 22 теста прошли. Проверены длины полей, обязательное согласие и отказ при передаче `email`/`company`, даже пустых.
- API: 37 тестов прошли на отдельной PostgreSQL 17 (`povod_test`, порт 15432). Включены конкурентная идемпотентность, конкурирующие workers, откат при ошибке outbox, retry/stale locks, формы без комментария и отказ при отсутствии имени/телефона.
- Без Docker встроенная PostgreSQL: 35 API-тестов прошли, 2 проверки межсоединительной конкуренции пропущены и затем отдельно пройдены на реальной PostgreSQL.
- Typecheck `@gift/contracts` и `@gift/api`, API lint и сборка API/worker/migrate прошли.
- Новый тест миграции загружает исходную `0000`, создаёт старую заявку с уведомлением, применяет `0001` и проверяет полное сохранение всех оставшихся полей и outbox. Имя/телефон остаются `NOT NULL`, комментарий nullable.
- Backup текущей рабочей БД сохранён в `.cache/backups/sladkiy-dar-before-lead-fields-20260911.dump` (8980 байт). Архив проверен восстановлением в отдельную PostgreSQL-БД; `0001` применена к копии. Количество и хэши всех сохраняемых полей заявок и полного outbox совпали, две старые колонки удалены. Проверочная копия после проверки удалена.
- До runtime-обновления рабочая БД содержала 1 заявку, 1 запись outbox и 1 применённую миграцию. После обновления исходные данные сохранены; журнал содержит 2 миграции. Backup и обезличенные manifests `.cache/backups/before-lead-fields.json` и `.cache/backups/after-lead-fields.json` сохранены.

## Обновлённый сайт по ТЗ — браузерные проверки 11 сентября 2026

- Финальный прогон Playwright в установленном Chrome: **17 из 17 тестов прошли** за 29,3 с на dev-сервере `http://127.0.0.1:5174`, после обновления изображений и исправления вёрстки.
- Проверены маршруты `/`, `/catalog`, `/contacts`, `/privacy`, `/agreement` на семи ширинах: 375, 430, 768, 1024, 1280, 1440 и 1920 px. На каждой странице один H1, title и SEO-метаданные; нет горизонтального переполнения, ошибок JavaScript и битых загруженных изображений.
- Дополнительно измерены границы hero-карусели на каждой ширине: блок имеет положительную ширину и полностью помещается между левой и правой границами окна. Эта проверка обнаруживает выход hero за экран даже при скрытом переполнении родителя.
- Подтверждено исправление переполнения главной на 375 px (декоративное сердце) и обрезания hero на 768 px.
- Проверены общий диалог для 12 основных контекстных CTA и финального блока, запрос из каталога, мобильное меню с переходом в форму, циклический Tab, Escape и возврат фокуса к кнопке.
- Имя, телефон и согласие обязательны. Заявка с пустым комментарием принимается; `email` и `company` отсутствуют в интерфейсе и отправляемом JSON. При неопределённом ответе сервера повторно отправляется неизменённый payload с прежним ID и сохранённым комментарием.
- Витрина управляется клавиатурой; выбор последнего набора показывает его состав и количество изделий. Запрос цены сохраняет источник выбранного набора `showcase-product:big-occasion`.
- Проверены сочетание фильтров каталога, пустая выдача и сброс фильтров; переходы между политикой и соглашением; отсутствие публичных сумм оптовых порогов и условий оплаты в продающих страницах.
- Браузерные сценарии отправки перехватывают `/api/leads` для воспроизводимой проверки интерфейса. Реальное сохранение и миграция БД проверяются отдельно в API-тестах и runtime smoke.
- Файлы сценариев: `tests/e2e/site.spec.ts`. HTML-отчёт последнего прогона: `playwright-report/index.html`.

Следующие результаты относятся к предыдущей версии сайта от 10 сентября; результаты нового браузерного прохода приведены выше.

## Предыдущая версия: код и пользовательские сценарии

- ESLint: успешно.
- TypeScript: успешно во всех workspace packages.
- Production build: успешно; API, worker, migrations и web assets.
- Contracts: 9 тестов.
- API: 22 теста на настоящем PostgreSQL 17, включая 12 одновременных запросов с одинаковым ID, competing worker claims, transaction rollback, stale locks, Telegram 429 и безопасный 404.
- Frontend: 10 тестов — validation, success/error, очищение, защита двойной отправки, сохранение полного payload при неопределённом сетевом результате, modal и каталог.
- Playwright: 14 тестов, все прошли в установленном Chrome. Для этого окружения использован PLAYWRIGHT_CHANNEL=chrome; по умолчанию конфигурация использует устанавливаемый Playwright Chromium.
- Все пять основных маршрутов проверены на 375, 430, 768, 1024, 1280, 1440, 1920 px: один H1, правильный title, отсутствие horizontal overflow, ошибок JavaScript и битых загруженных изображений.

## Визуальный и accessibility pass

Реально просмотрены снимки desktop главной и полного лендинга, мобильного первого экрана, desktop/mobile каталога, кейсов, детали кейса, контактов и privacy. Снимки находятся в локальном .cache и не требуются приложению.

Исправлены склеенные строки после скрытия br на mobile, композиция последнего кейса, замыкание Tab в modal, контраст светлого текста на красном, контраст текста на розовом и подчёркивание ссылки privacy.

axe-core 4.10.3 с правилами wcag2a / wcag2aa / wcag21aa: 0 нарушений в восьми проверках (шесть страниц desktop + homepage mobile + modal mobile). Клавиатурное открытие/закрытие, возврат фокуса, Escape и циклический Tab дополнительно проверены Playwright. Это автоматический аудит и выбранные ручные сценарии, а не полная сертификация доступности.

## Загрузка

Финальный основной JS: 322.78 KB / 100.77 KB gzip. Общий CSS: 32.51 KB / 7.30 KB gzip. Дополнительные страницы вынесены в отдельные chunks. Manrope self-hosted, только Latin и Cyrillic (24.84 и 14.50 KB). Hero имеет fetchPriority=high, остальные изображения lazy; WebP варианты 768/1536. Production build принудительно использует production NODE_ENV, независимо от .env backend development.

## Что требует реальной конфигурации

Контакты и реквизиты бизнеса, согласованный текст обработки данных, подтверждённые фотографии/составы/цены/минимальная партия и домен заменяются перед коммерческой публикацией. Реальные VK credentials не предоставлены: HTTP-интеграция проверяется тестами, доставка реальному менеджеру требует VK_GROUP_TOKEN, VK_GROUP_ID и VK_PEER_ID. Порядок настройки — в [инструкции VK](vk-setup.md). Демонстрационные кейсы не выдаются за выполненные клиентские заказы.

Фактическая проверка Docker и сохранности volume записана в README после завершения production smoke.

## Визуальная проверка и доступность новой версии

- Главная просмотрена на 1440, 768 и 375 px, включая все блоки, изображения и форму. Картинки склада и менеджера обозначены как иллюстрации; отзывы скрыты до появления реальных.
- axe-core 4.10.3: нет нарушений WCAG 2 A/AA и 2.1 AA на `/`, `/catalog`, `/contacts`, `/privacy`, `/agreement`, `/cases`, `/cases/team-gifts` и в форме на 375 px. Автоматический аудит не заменяет ручную проверку клавиатуры, проведённую в Playwright.
- Исправлен контраст подписей витрины, планшетная компоновка hero и декоративное переполнение счётчика. Форма без раскрытого комментария целиком помещается на экране 375 × 812 px (высота диалога около 752 px); при раскрытии или меньшей высоте диалог прокручивается.
- Frontend Vitest: 13 тестов прошли. Общий ESLint, TypeScript и сборка монорепозитория прошли. Визуальные артефакты и результаты локального аудита сохранены в ignored `.cache/v2-*.jpg` и `.cache/qa-v2.json`.

## Обновление рабочего Docker stack — 11 сентября 2026

- Новые Linux-образы API и web собраны Docker Desktop через ASCII junction. Миграция `0001` завершилась с кодом 0; сверка `After` выполнена **11 сентября в 11:17:56 МСК** до запуска нового worker.
- Количество исходных записей осталось прежним: 1 заявка и 1 outbox. Хэши всех сохраняемых полей заявки и полного outbox совпали с проверенным backup. В `leads` отсутствуют `email`/`company`, `name`/`phone` остаются `NOT NULL`, `message` допускает `NULL`. Журнал содержит 2 миграции.
- Новый API через Caddy на `http://localhost` проверен в 11:19 МСК: `/api/health/live` и `/api/health/ready` возвращают 200; `/`, `/catalog`, `/contacts`, `/privacy`, `/agreement` отвечают 200.
- Запросы без имени, телефона или согласия, а также запросы со старыми `email` или `company` получили 400 без записи в БД. Синтетическая заявка с именем, телефоном и пустым комментарием получила 201, повтор с тем же `clientRequestId` — 200 и прежний ID; создан ровно один outbox. SQL подтвердил нормализованные имя/телефон и `message IS NULL`.
- Во время синтетического smoke worker был остановлен: тест не отправил сообщений Telegram. Удалены только заявка и outbox с собственным UUID запроса и источником `production-form-v2-synthetic`; хэши исходных данных после очистки совпали с состоянием до теста.
- Worker запущен после отдельной проверки отсутствия обоих Telegram-параметров; значения секретов не выводились. Без credentials доставка не выполняется, существующий outbox остаётся для последующих попыток.
- PostgreSQL, API и web имеют статус healthy; Caddy и worker работают; migrate завершён с кодом 0. При первом запросе сразу после холодного старта Caddy соединение сбросилось; после HTTP readiness проверка прошла, изменений приложения для этого не потребовалось.
- Обезличенные результаты runtime smoke: `.cache/production-form-v2-smoke.json`. Проверенный backup: `.cache/backups/sladkiy-dar-before-lead-fields-20260911.dump`.

Финальная проверка через Caddy (`http://localhost`): все 9 маршрутов возвращают 200, содержат один H1 и единственные description/canonical; все изображения загружаются, ошибок JavaScript нет. Форма на 375 px проверена в рабочей Docker-сборке: имя и телефон required, email/company отсутствуют. Итоговый декор индивидуального набора (кружка, открытка, лента) присутствует в развёрнутом web. Результаты: `.cache/production-web-v2.json`.

## Рабочая Docker-сборка с VK — 11 сентября 2026, 16:24–16:27 МСК

- Собраны и запущены новые Linux-образы web и API. PostgreSQL, API и web healthy; Caddy и worker работают; migrate завершён с кодом 0.
- Перед изменением схемы остановлены принимающие/отправляющие сервисы. Backup .cache/backups/sladkiy-dar-before-vk-20260911-162157.dump (9072 байта) проверен восстановлением в отдельную БД и применением 0002 к копии.
- В рабочей БД сохранены обе исходные заявки и обе записи outbox; совпадение полей проверено хэшами. Две ожидавшие записи перенесены на vk.lead.created, random_id уникальны. Журнал содержит три миграции. Обезличенные manifests — .cache/backups/before-vk.json и after-vk.json.
- Runtime smoke подтвердил 400 для отсутствующих имени/телефона/согласия и удалённых email/company. Новая синтетическая заявка получила 201, повтор — 200 и прежний ID. Создан один outbox с типом vk.lead.created и random_id = 3; пустой комментарий сохранён как NULL.
- Во время smoke worker был остановлен; реальных сообщений не отправлено. Удалены только собственные синтетические данные, исходные записи после очистки неизменны. Результаты — .cache/production-vk-smoke.json.
- Worker запущен после проверки пустых VK_GROUP_TOKEN / VK_GROUP_ID / VK_PEER_ID. Старых переменных транспорта в окружении worker нет. Реальная доставка ожидает настройки владельцем.
- Финальный браузерный smoke через Caddy на http://localhost: 9/9 маршрутов отвечают 200, изображения исправны, H1/description/canonical единственные. Подтверждены пять карточек поводов, новые заголовки, отсутствие старой hero-плашки и удалённых поводов, перетаскивание мышью и следующий обычный клик. Мобильная форма сохраняет обязательные имя/телефон, email/company отсутствуют, ошибок JavaScript нет. Результаты — .cache/production-web-v3.json.

## Разделение ролей PostgreSQL — 11 сентября 2026, 17:16–17:26 МСК

- API и worker переведены на разные LOGIN-роли без SUPERUSER, CREATEDB, CREATEROLE, REPLICATION и BYPASSRLS. В рабочей Docker-сборке это povod_api и povod_worker.
- API получает INSERT в leads/outbox и SELECT только id/client_request_id в leads для возврата ответа и идемпотентности. Чтение имени/телефона/комментария из БД, изменения и удаления ранее принятых записей запрещены.
- Worker получает SELECT заявок/очереди и UPDATE только столбцов состояния доставки. INSERT, DELETE, TRUNCATE, DDL, временные таблицы, доступ к drizzle и повышение привилегий запрещены обеим runtime-ролям.
- Provisioning выполняется административным migrate после миграций схемы. Выдача прав транзакционная и повторяемая, роли защищены от случайного переиспользования чужих/привилегированных аккаунтов. Будущим таблицам не выдаются автоматические широкие права.
- Compose передаёт каждому runtime-контейнеру только его DATABASE_URL; административные реквизиты и пароль соседней роли в API/worker отсутствуют. VK-токен по-прежнему есть только у worker. Для native запуска настроены DATABASE_URL, WORKER_DATABASE_URL и MIGRATION_DATABASE_URL, роли применены также к dev-БД.
- Полный backend-прогон: **80/80 тестов прошли** на отдельной PostgreSQL. Включены 10 новых тестов с настоящей аутентификацией ограниченных ролей, рабочими INSERT/identity/идемпотентностью/очередью и ожидаемыми отказами доступа. Проверены кавычки в реквизитах, повторная настройка и отзыв ранее выданных прав на отдельные столбцы. Одноразовые БД и роли после тестов удалены.
- Общие TypeScript, ESLint и format:check прошли; финальный Linux-образ API/worker/migrate собран и развёрнут.
- Перед рабочим изменением остановлены API и worker. Backup .cache/backups/sladkiy-dar-before-db-roles-1789136216210_8d94cb.dump проверен восстановлением в отдельную БД и двукратным provisioning с отдельными проверочными ролями. Все 4 заявки и 4 записи очереди совпали по хэшам, исходная БД при проверке копии неизменна; копия и проверочные роли удалены.
- Рабочий migrate завершился с кодом 0. После provisioning в production количество и полные хэши заявок/очереди сохранены; журнал схемы по-прежнему содержит 3 миграции. Смена прав не требует изменения строк заявок или новой миграции таблиц.
- Production smoke через Caddy и реальную ограниченную API-роль: создание 201, повтор 200, один outbox со стабильным идентификатором, пустой комментарий принят. Worker во время этой проверки остановлен, VK-сообщений не отправлено; удалена только собственная синтетическая заявка, исходные данные после очистки неизменны.
- Проверены уже работающие API и worker: current_user соответствует отдельной роли, административные флаги отключены, административных переменных окружения нет, безопасные запросы DELETE ... WHERE false возвращают permission denied. API также получает отказ при попытке чтения phone, worker — при попытке изменения leads. API/web/PostgreSQL healthy; Caddy и worker работают.
- Обезличенные результаты: .cache/db-roles-backup-check.json, .cache/backups/before-db-roles.json, .cache/backups/after-db-roles.json, .cache/production-db-roles-smoke.json и .cache/production-db-roles-runtime.json. Порядок эксплуатации и восстановления описан в [database-access.md](database-access.md).
